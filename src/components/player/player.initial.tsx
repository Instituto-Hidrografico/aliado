import { Player } from "./player.interface";
import { pieceInitial } from "../piece/piece.initial";

export const playerInitial : Player = {
    name: "GG",
    piece: pieceInitial,//[pieceInitial, pieceInitial, pieceInitial, pieceInitial],
}