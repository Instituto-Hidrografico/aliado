import { Field } from "./field.interface";
import { pieceInitial } from "./piece.initial";

export const fieldInitial : Field = {
    piece: [pieceInitial, pieceInitial, pieceInitial, pieceInitial, pieceInitial, pieceInitial],
}